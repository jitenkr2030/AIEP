"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const index_ts_1 = require("../index.js");
const schema = index_ts_1.i.schema({
    entities: {
        tbl: index_ts_1.i.entity({
            d: index_ts_1.i.date().indexed(),
            dOptional: index_ts_1.i.date().optional(),
        }),
    },
});
async function _testUseDatesTest() {
    const db = (0, index_ts_1.init)({
        schema,
        appId: '123',
        useDateObjects: true,
    });
    const data = await db.query({ tbl: {} });
    const item = data?.tbl[0];
    if (item) {
    }
}
async function _testUseDatesFalseTest() {
    const db = (0, index_ts_1.init)({
        schema,
        appId: '123',
        useDateObjects: false,
    });
    const data = await db.query({ tbl: {} });
    const item = data?.tbl[0];
    if (item) {
    }
}
async function _testUseDatesUndefinedTest() {
    const db = (0, index_ts_1.init)({
        schema,
        appId: '123',
    });
    const data = await db.query({ tbl: {} });
    const item = data?.tbl[0];
    if (item) {
    }
}
async function _testDataNoSchema() {
    const db = (0, index_ts_1.init)({
        appId: '123',
    });
    const data = await db.query({ tbl: {} });
    const item = data?.tbl[0];
    if (item) {
    }
}
async function _testSubscribe() {
    // No useDateObjects
    (0, index_ts_1.init)({
        appId: '123',
        schema,
    }).subscribeQuery({ tbl: {} }, (payload) => {
        if (payload.type === 'ok') {
            const item = payload.data.tbl[0];
            if (item) {
            }
        }
    });
    // UseDateObjects
    (0, index_ts_1.init)({
        appId: '123',
        schema,
        useDateObjects: true,
    }).subscribeQuery({ tbl: {} }, (payload) => {
        if (payload.type === 'ok') {
            const item = payload.data.tbl[0];
            if (item) {
            }
        }
    });
    // No schema
    (0, index_ts_1.init)({
        appId: '123',
    }).subscribeQuery({ tbl: {} }, (payload) => {
        if (payload.type === 'ok') {
            const item = payload.data.tbl[0];
            if (item) {
            }
        }
    });
}
//# sourceMappingURL=typesTests.js.map